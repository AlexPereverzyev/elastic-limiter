const { expect } = require('chai').use(require('chai-as-promised'))

const ElasticCounter = require('../')

describe('elastic-counter', () => {
    it('should be OK when ever', async () => {
        // todo
        expect(true).is.true
    })
})
